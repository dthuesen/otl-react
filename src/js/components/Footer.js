import React from "react";
import ReactDOM from "react-dom";

// Composing
import Footer from "./Header/Brand";

export default class Header extends React.Component {
  render() {
    return ( 
      <div className="container">
        Footer component
      </div>
    );
  }
}
